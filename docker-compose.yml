version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3001"
    volumes:
      - ./downloads:/app/downloads
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
